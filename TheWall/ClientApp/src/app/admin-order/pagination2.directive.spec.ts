import { Pagination2Directive } from './pagination2.directive';

describe('Pagination2Directive', () => {
  it('should create an instance', () => {
    const directive = new Pagination2Directive();
    expect(directive).toBeTruthy();
  });
});
